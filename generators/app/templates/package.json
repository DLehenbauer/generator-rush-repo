{
  "name": "<%= npmScope %>/root",
  "repository": {
    "type": "git",
    "url": "<%= repoUrl %>"
  },
  "author": "<%= author %>",
  "license": "<%= license %>",
  "scripts": {
    "build": "npm run rush -- build",
    "clean": "git clean -Xfd",
    "postinstall": "npm run rush -- install",
    "prepublishOnly": "npm run build && npm run test",
    "rush": "node common/scripts/install-run-rush.js",
    "rush:repair": "npm run clean && (rm common/config/rush/pnpm-lock.yaml || true) && npm run rush -- update --purge --recheck",
    "test": "npm run rush -- test"
  }
}
